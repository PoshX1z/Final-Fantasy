import React from "react";

export const SearchBarStore = () => {
  return (
    <div>
      <input
        type="text"
        placeholder="Search product in store..."
        className="border-2 border-primary w-full"
      />
    </div>
  );
};
